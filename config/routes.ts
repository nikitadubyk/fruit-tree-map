const AUTH = {
  LOGIN: '/login',
  MAGIC_LINK: '/magic-link',
};

const PROFILE = {
  HOME: '/profile',
};

const APPROVE = {
  HOME: '/approve',
};

export const ROUTES = {
  AUTH,
  PROFILE,
  APPROVE,
  HOME: '/',
};
